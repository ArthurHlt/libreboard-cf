(function() {
    if (Meteor.isClient) {
    
        // Profile,
        Helpers("profile", {});
        Rendered("profile", function(addClass) {

            // rendered
        });

        Template.profile.events({
            // profile
        });
    }
}());
